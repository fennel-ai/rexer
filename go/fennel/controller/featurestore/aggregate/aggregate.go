package aggregate

import (
	"context"

	"fennel/featurestore/tier"
	lib "fennel/lib/featurestore/aggregate"
)

func Store(ctx context.Context, tier tier.Tier, agg lib.Aggregate) error {
	return nil
}
